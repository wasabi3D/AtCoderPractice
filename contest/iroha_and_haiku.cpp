#include <iostream>
#include <vector>
using namespace std;
using ll = long long;


int main(){
    ll N, P, Q, R; cin >> N >> P >> Q >> R;
    vector<ll> a(N);
    for(ll i = 0; i < N; i++) cin >> a.at(i);

    ll x, y, z, w;
    ll sum_x = 0;
    for(x = 0; )
}